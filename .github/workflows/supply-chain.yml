name: Supply chain

# Guards:
#   1. Dependency review on every PR (fails on new deps or license changes
#      that violate policy).
#   2. OSSF Scorecard run weekly to produce a supply-chain health score.
#   3. SBOM refresh weekly so main always has a current CycloneDX SBOM
#      uploaded as an artifact.
#
# Policy lives in docs/release/SUPPLY-CHAIN.md.

on:
  pull_request:
  schedule:
    # Sundays at 09:00 UTC.
    - cron: '0 9 * * 0'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dependency-review:
    name: Dependency review (PRs)
    # TODO(ci): flip `false` → `github.event_name == 'pull_request'`
    # once `actions/dependency-review-action` stops reporting
    # "not supported on this repository". Enabling Dependency Graph in
    # repo settings is necessary but not sufficient — GitHub Advanced
    # Security (free for public repos but separately toggled at
    # Settings → Code security) must also be on. Using `if: false`
    # here skips the job entirely so the check does not surface as
    # perpetually red on every PR.
    if: false
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          # Fail the PR if any dep has a vuln at "moderate" or higher.
          fail-on-severity: moderate
          # Disallow licenses that are incompatible with MIT OR Apache-2.0.
          deny-licenses: GPL-2.0, GPL-3.0, AGPL-3.0, LGPL-2.1, LGPL-3.0
          comment-summary-in-pr: on-failure

  scorecard:
    name: OSSF Scorecard
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Run Scorecard
        uses: ossf/scorecard-action@v2.4.3
        with:
          results_file: scorecard-results.sarif
          results_format: sarif
          publish_results: true

      - name: Upload Scorecard artifact
        uses: actions/upload-artifact@v4
        with:
          name: scorecard-results
          path: scorecard-results.sarif
          retention-days: 30

      - name: Upload Scorecard results to code scanning
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: scorecard-results.sarif

  sbom-refresh:
    name: Refresh SBOM
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Generate CycloneDX SBOM
        uses: anchore/sbom-action@v0
        with:
          path: .
          format: cyclonedx-json
          output-file: sbom.cdx.json
          artifact-name: sbom.cdx.json
          upload-artifact: false

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v4
        with:
          name: weekly-sbom
          path: sbom.cdx.json
          retention-days: 90
