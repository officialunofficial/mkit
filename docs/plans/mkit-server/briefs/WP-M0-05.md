> **SPLIT (review 01, 00-plan.md R-72).** This work package no longer exists. It was split into:
>
> - [WP-M0-05a](WP-M0-05a.md): pipeline core (stage traits, auth modes, `ShardMap`, pure planners with bounded
>   re-plan and `NotAfter` deadlines, unary RPC flow). Depends on M0-02a, M0-04.
> - [WP-M0-05b](WP-M0-05b.md): `UploadSession`, `DownloadStream`, resumable `UploadPack`, `test-faults` seam.
>   Depends on M0-05a.
>
> This stub is kept so old references resolve; it is not in `registry.json` and must not be scheduled.
