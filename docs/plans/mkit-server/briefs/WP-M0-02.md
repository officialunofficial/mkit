> **SPLIT (review 01, 00-plan.md R-72).** This work package no longer exists. It was split into:
>
> - [WP-M0-02a](WP-M0-02a.md): storage contract core (key-level `NamespaceStore` with `NotAfter` deadlines, key
>   layouts, codecs, typed readers, `BlobStore` trait, replay model, memory backends). Depends on M0-01.
> - [WP-M0-02b](WP-M0-02b.md): `ContentIndex` layer, portable export/import, optional `StoreMaintenance` /
>   `StateCommitment` hooks. Depends on M0-02a.
>
> This stub is kept so old references resolve; it is not in `registry.json` and must not be scheduled.
