//! Internal test-only crate: the tests in `tests/` run mkit-core's
//! `pack-ruzstd` decoder on `wasm32-unknown-unknown` (a 32-bit `usize`)
//! and natively. There is no library API; see `Cargo.toml`.
