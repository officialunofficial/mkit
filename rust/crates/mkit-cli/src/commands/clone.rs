//! `mkit clone <url>` — initialise a new repo and pull from the URL.
//! Phase 10 follow-up: shallow clone, sparse patterns.

#[must_use]
pub fn run(_args: &[String]) -> u8 {
    super::not_yet_ported("clone")
}
