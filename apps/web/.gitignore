node_modules
dist
.wrangler
.vite
wrangler.jsonc
src/pages.gen.ts
.env*
# …but DO commit .env.production: it holds only the public, build-time
# VITE_REPO_BACKEND_URL (baked into the client bundle anyway), so the multiplayer
# demo points at the shared repo Worker without a per-deploy dashboard env.
!.env.production
*.tsbuildinfo
.cache
.DS_Store
*.pem
vendor/mkit-wasm/pkg/
# Staged at build time from the canonical repo-root files (scripts/stage-public.mjs)
public/SKILL.md
public/install.sh
# Generated at build time from src/security-policy.ts (scripts/gen-headers.mjs)
public/_headers
