---
layout: docs
title: Introduction
description: BLAKE3-addressed objects, Ed25519-signed commits, portable in-toto v1 attestations. A Git-shaped VCS, rebuilt from first principles.
---

# mkit

A content-addressed version control toolkit written in Rust.

`mkit` is a generic content-addressed VCS — Git-like commits, refs, and
transports — plus a native, predicate-agnostic attestation subsystem
(in-toto v1 Statements wrapped in DSSE envelopes) that any downstream
service can attach witness signatures to commits with.

The v1 on-disk and wire formats are pinned by golden vectors under
`rust/tests/golden/` in the source repository.

```bash
cargo install --git https://github.com/officialunofficial/mkit mkit-cli
```

## Start here

- [Install](/docs/install) — four channels (source, releases, npm/WASM,
  hardware signers) with verification steps.
- [CLI reference](/docs/cli) — subcommands, config keys, URL schemes,
  exit codes, environment variables, signals.
- [Architecture](/docs/architecture) — code map of the Rust workspace
  and the data flow between objects, packs, refs, transports, and
  attestations.

## Specifications

The on-disk and wire formats are normative. Read these if you are
implementing a compatible tool, a new transport, or a verifier.

**Repository format** — [Objects](/docs/spec/objects),
[Staging index](/docs/spec/staging-index),
[Refs](/docs/spec/refs).

**Pack format** — [Packfile](/docs/spec/packfile),
[Delta](/docs/spec/delta),
[FastCDC](/docs/spec/fastcdc).

**Wire protocols** — [Transport](/docs/spec/transport),
[RPC](/docs/spec/rpc).

**Signing & attestations** — [Signing](/docs/spec/signing),
[Attestations](/docs/spec/attestations),
[External signer](/docs/spec/external-signer).

## Operations

- [Release process](/docs/release) — maintainer checklist for cutting
  a tagged release.
- [Fuzzing](/docs/fuzz) — fuzz-harness conventions and guardrails.
- [SSH transport security](/docs/ssh-security) — trust model for the
  `mkit+ssh://` transport.
- [Threat model](/docs/threat-model) — security boundaries and
  attacker capabilities mkit defends against.

## Try it in your browser

Five interactive demos backed by the `mkit-wasm` build:
[hash](/demos/hash), [sign](/demos/sign), [attest](/demos/attest),
[tree](/demos/tree), [streaming](/demos/streaming).
