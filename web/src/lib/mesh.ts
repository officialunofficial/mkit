// Soft brand mesh gradient for the /push surface. Shared by the landing tile
// (index.tsx MESH) and the "Road B" accent card in push.tsx so the accent
// reads consistently and a palette change lives in one place.
export const PUSH_MESH =
  'radial-gradient(at 18% 20%, rgba(202,138,4,0.10), transparent 55%), radial-gradient(at 84% 82%, rgba(0,210,168,0.07), transparent 55%)'
